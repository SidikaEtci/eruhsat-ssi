# Default municipality when the UI does not override (all 81 provinces supported)
CITY_SLUG=konya

# Allow selecting any municipality in the web UI (true/false)
MULTI_CITY=true

# Public URL used in QR codes
PUBLIC_BASE_URL=http://localhost:8000

# IPFS daemon
IPFS_HOST=127.0.0.1
IPFS_PORT=5001
